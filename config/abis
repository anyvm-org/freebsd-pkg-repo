# slug                freebsd_version   poudriere_arch   jail
# The freebsd_version here pins the riscv64 TARGET jail, which needs a
# concrete base.txz URL. The amd64 build host is selected separately in
# build.yml with a major-version prefix.
FreeBSD-15-riscv64    15.1-RELEASE      riscv.riscv64    rv64
# BLOCKED (2026-09-02): emulators/qemu-user-static 3.1.0 aborts on every
# FreeBSD 15 powerpc64 binary ("Assertion failed: (!have_mmap_lock())"),
# qemu-user-static-devel has no package on 15, and a native ppc64 guest
# is pinned to one TCG CPU. The row stays so the ABI is reserved; do not
# dispatch it expecting packages.
FreeBSD-15-powerpc64  15.1-RELEASE      powerpc.powerpc64  ppc64
